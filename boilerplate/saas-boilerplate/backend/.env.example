# Environment Variables Template
# Copy to .env and fill in your values
# The app starts without most of these — missing vars log warnings, not crashes.

# ─── Server ───────────────────────────────────────────────────────────────────
PORT=8000
ENVIRONMENT=development            # development | production

# ─── CORS ─────────────────────────────────────────────────────────────────────
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# ─── Auth0 ────────────────────────────────────────────────────────────────────
# Create a Single Page Application + an API in your Auth0 dashboard.
# Set Allowed Callback URLs: http://localhost:3000
# Set Allowed Logout URLs: http://localhost:3000
# Set Allowed Web Origins: http://localhost:3000
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_CLIENT_ID=your_client_id_here
AUTH0_CLIENT_SECRET=your_client_secret_here
AUTH0_AUDIENCE=https://your-tenant.auth0.com/api/v2/

# ─── Stripe ───────────────────────────────────────────────────────────────────
# Stripe Dashboard → Developers → API Keys
STRIPE_SECRET_KEY=sk_test_your_key_here
# Stripe Dashboard → Developers → Webhooks → Add endpoint → Signing secret
# Webhook URL: https://yourdomain.com/api/webhooks/stripe
# Events: charge.succeeded, customer.subscription.created,
#         customer.subscription.deleted, invoice.payment_succeeded,
#         charge.dispute.created, radar.early_fraud_warning.created
STRIPE_WEBHOOK_SECRET=whsec_your_secret_here

# ─── Sentry (error tracking) ──────────────────────────────────────────────────
# sentry.io → Create Python project → Settings → Client Keys → DSN
SENTRY_DSN=https://xxxxx@oyyy.ingest.sentry.io/zzzz
ENV=development                    # Environment tag in Sentry
APP_VERSION=4.0.0                  # Release tag in Sentry
SENTRY_TRACES_SAMPLE_RATE=0.1      # 0.0–1.0 (10% = 0.1)

# ─── MailerLite (email) ───────────────────────────────────────────────────────
# mailerlite.com → Integrations → API
MAILERLITE_API_KEY=your_api_key_here

# ─── Google Analytics (optional) ──────────────────────────────────────────────
# analytics.google.com → Data Streams → Measurement Protocol API secrets
GA4_MEASUREMENT_ID=G-XXXXXXXXXX
GA4_API_SECRET=your_api_secret_here

# ─── GitHub (optional, for git_lib) ───────────────────────────────────────────
# github.com → Settings → Developer settings → Personal access tokens
GITHUB_TOKEN=your_github_token_here
